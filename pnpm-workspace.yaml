# Supply-chain posture (also enforced in the Docker build):
# - packages must be ≥24h old at resolution time
# - only these packages may run install scripts
minimumReleaseAge: 1440
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
